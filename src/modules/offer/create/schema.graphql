scalar Upload

input CreateOfferInput {
  title: String!
  description: String!
  picture: Upload
  category: String!
  latitude: Float!
  longitude: Float!
}

type Mutation {
  createOffer(input: CreateOfferInput!): Boolean!
}
